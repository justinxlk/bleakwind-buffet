<UserControl x:Class="PointOfSale.ItemCustomization.CustomGardenOrcOmelette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale.ItemCustomization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="3" TextAlignment="Center" FontSize="30">Customize Garden Orc Omelette</TextBlock>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="3">
            <CheckBox FontSize="25" IsChecked="{Binding Path=Broccoli}" BorderThickness="10">Broccoli</CheckBox>
            <CheckBox FontSize="25" IsChecked="{Binding Path=Mushroom}" BorderThickness="10">Mushroom</CheckBox>
            <CheckBox FontSize="25" IsChecked="{Binding Path=Tomato}" BorderThickness="10">Tomato</CheckBox>
            <CheckBox FontSize="25" IsChecked="{Binding Path=Cheddar}" BorderThickness="10">Cheddar</CheckBox>
        </StackPanel>   
    </Grid>
</UserControl>
