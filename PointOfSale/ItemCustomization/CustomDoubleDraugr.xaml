<UserControl x:Class="PointOfSale.ItemCustomization.CustomDoubleDraugr"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale.ItemCustomization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="3" TextAlignment="Center" FontSize="30">Customize Double Draugr</TextBlock>
        <Button Grid.Column="2" Grid.Row="2" FontSize="30" Click="ClickDone">Done</Button>
        <Button Grid.Column="0" Grid.Row="2" FontSize="30" Click="ClickCancel">Cancel</Button>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="3">
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Bun</CheckBox>
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Ketchup</CheckBox>
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Mustard</CheckBox>
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Pickle</CheckBox>
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Cheese</CheckBox>
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Tomato</CheckBox>
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Lettuce</CheckBox>
            <CheckBox FontSize="25" IsChecked="True" BorderThickness="10">Mayo</CheckBox>
        </StackPanel>
    </Grid>
</UserControl>
